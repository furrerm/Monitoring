package entities;

import sender.MessageData;

import java.util.HashSet;
import java.util.Set;

public class EntityRefinerImpl{
/*
    private sender.MessageData entityToMessageData(DataEntity dataEntity) {
        sender.MessageData message = new MessageData(dataEntity.getUuid());
        message.setTime(dataEntity.getTime());

        message.setKorb(dataEntity.getKorb());
        message.setGui(dataEntity.getGui());
        message.setAmbulant(dataEntity.getAmbulant());
        message.setStationaer(dataEntity.getStationaer());
        message.setPartnerartObergruppe(dataEntity.getPartnerartObergruppe());
        message.setPlz(dataEntity.getPlz());
        message.setIn(dataEntity.getIncoming());
        message.setOut(dataEntity.getOutgoing());
        message.setKorbStand(dataEntity.getKorbStand());
        return message;
    }

    @Override
    public Set<sender.MessageData> entitiesToMessageData(Set<DataEntity> dataEntities) {
        Set<sender.MessageData> messages = new HashSet<>();
        for (DataEntity dataEntity : dataEntities) {
            messages.add(this.entityToMessageData(dataEntity));
        }
        return messages;
    }
    */
}
